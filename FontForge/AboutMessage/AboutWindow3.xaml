<Window x:Class="FontForge.AboutMessage.AboutWindow3"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="О программе"
        Height="327" Width="540"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}"
        Icon="/Images/Logo.png"
        Opacity="0"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="18" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DotStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Height" Value="8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Fill" Value="{DynamicResource SecondaryTextBrush}"/>
            <Setter Property="Opacity" Value="0.35"/>
        </Style>

        <Style x:Key="DotActiveStyle" TargetType="Ellipse" BasedOn="{StaticResource DotStyle}">
            <Setter Property="Opacity" Value="1"/>
            <Setter Property="Fill" Value="{DynamicResource TextBrush}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        

        <!-- Заголовок -->
        <TextBlock Text="Редактируй размер букв"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,45,0,0"
                   VerticalAlignment="Top"/>

        <!-- Текст -->
        <TextBlock Text="Контролируйте пропорции и размеры символов.
Программа помогает выравнивать высоту, ширину
и выносные элементы, обеспечивая
комфортное чтение текста."


                   FontSize="15"
                   Foreground="{DynamicResource SecondaryTextBrush}"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   Width="445"
                   HorizontalAlignment="Center"
                   Margin="0,100,0,0"
                   VerticalAlignment="Top"/>

        <!-- Стрелка назад -->
        <Button Style="{StaticResource IconButtonStyle}"
                Content="❮"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Margin="0,0,0,10"
                Click="PrevButton_Click"/>

        <!-- Точки снизу -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Margin="0,0,0,18">
            <Ellipse Style="{StaticResource DotStyle}"/>
            <Ellipse Style="{StaticResource DotStyle}"/>
            <Ellipse Style="{StaticResource DotActiveStyle}"/>
        </StackPanel>
    </Grid>
</Window>
